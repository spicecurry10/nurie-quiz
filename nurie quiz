<!DOCTYPE html>
<html lang="ja">
<head>
<meta charset="UTF-8">
<meta name="viewport" content="width=device-width, initial-scale=1.0, viewport-fit=cover">
<title>みわの ぬりえクイズ</title>
<style>
  :root {
    --bg1: #fff4fb;
    --bg2: #eef8ff;
    --card: #ffffff;
    --ink: #4a3b4f;
    --muted: #7b6f80;
    --pink: #ff8cc6;
    --blue: #83c9ff;
    --purple: #b99cff;
    --yellow: #ffe08a;
    --green: #9be7b0;
    --red: #ff8f8f;
    --shadow: 0 14px 34px rgba(119, 76, 128, 0.18);
  }

  * {
    box-sizing: border-box;
    -webkit-tap-highlight-color: transparent;
    -webkit-touch-callout: none;
    user-select: none;
  }

  body {
    margin: 0;
    min-height: 100vh;
    font-family: -apple-system, BlinkMacSystemFont, "Hiragino Sans", "Hiragino Kaku Gothic ProN", "Yu Gothic", Meiryo, sans-serif;
    color: var(--ink);
    background:
      radial-gradient(circle at 15% 10%, rgba(255, 203, 232, 0.8), transparent 24%),
      radial-gradient(circle at 88% 8%, rgba(190, 230, 255, 0.9), transparent 25%),
      linear-gradient(135deg, var(--bg1), var(--bg2));
    padding: 16px;
    overflow-x: hidden;
  }

  .app {
    width: min(980px, 100%);
    margin: 0 auto;
  }

  .screen {
    display: none;
  }

  .screen.active {
    display: block;
  }

  .topbar {
    display: flex;
    justify-content: space-between;
    gap: 12px;
    align-items: center;
    margin-bottom: 12px;
  }

  .brand {
    display: flex;
    align-items: center;
    gap: 10px;
    font-weight: 900;
    font-size: clamp(24px, 5vw, 40px);
    letter-spacing: 0.03em;
  }

  .badge {
    background: rgba(255, 255, 255, 0.75);
    border: 2px solid rgba(255, 140, 198, 0.35);
    border-radius: 999px;
    padding: 8px 12px;
    font-weight: 800;
    color: #7b4b80;
    box-shadow: 0 8px 20px rgba(119, 76, 128, 0.12);
    white-space: nowrap;
  }

  .card {
    background: rgba(255,255,255,0.88);
    border: 3px solid rgba(255,255,255,0.9);
    border-radius: 30px;
    padding: clamp(16px, 3.5vw, 28px);
    box-shadow: var(--shadow);
    backdrop-filter: blur(8px);
  }

  .hero {
    display: grid;
    grid-template-columns: 1fr 1fr;
    gap: 22px;
    align-items: center;
  }

  .title {
    margin: 0 0 10px;
    font-size: clamp(34px, 7vw, 62px);
    line-height: 1.05;
    font-weight: 1000;
  }

  .subtitle {
    margin: 0 0 20px;
    font-size: clamp(18px, 3.2vw, 26px);
    line-height: 1.55;
    color: var(--muted);
    font-weight: 700;
  }

  .big-buttons {
    display: grid;
    gap: 12px;
  }

  button {
    border: 0;
    font: inherit;
    cursor: pointer;
  }

  .btn {
    min-height: 62px;
    border-radius: 22px;
    padding: 16px 18px;
    font-size: clamp(19px, 3.5vw, 26px);
    font-weight: 950;
    color: #fff;
    background: linear-gradient(135deg, #ff8cc6, #b99cff);
    box-shadow: 0 10px 22px rgba(185, 156, 255, 0.35);
    transform: translateY(0);
  }

  .btn:active {
    transform: translateY(2px) scale(0.99);
    box-shadow: 0 6px 14px rgba(185, 156, 255, 0.25);
  }

  .btn.secondary {
    color: #6b5080;
    background: #fff;
    border: 3px solid rgba(185,156,255,0.38);
    box-shadow: 0 8px 18px rgba(119, 76, 128, 0.12);
  }

  .btn.green {
    background: linear-gradient(135deg, #76dc99, #83c9ff);
  }

  .btn.yellow {
    color: #6a4b25;
    background: linear-gradient(135deg, #ffe08a, #ffbf75);
  }

  .btn.small {
    min-height: 48px;
    border-radius: 18px;
    font-size: 17px;
    padding: 11px 14px;
  }

  .paint-frame {
    background: #fff;
    border-radius: 28px;
    padding: 14px;
    box-shadow: inset 0 0 0 3px rgba(255, 140, 198, 0.14);
  }

  svg {
    width: 100%;
    height: auto;
    display: block;
  }

  [data-part] {
    transition: fill 450ms ease, transform 280ms ease;
  }

  .outline {
    stroke: #5c4b62;
    stroke-width: 5;
    stroke-linecap: round;
    stroke-linejoin: round;
  }

  .thin {
    stroke-width: 3.5;
  }

  .face {
    fill: none;
    stroke: #5c4b62;
    stroke-width: 4;
    stroke-linecap: round;
  }

  .theme-grid {
    display: grid;
    grid-template-columns: repeat(4, 1fr);
    gap: 11px;
    margin: 12px 0 18px;
  }

  .theme-card {
    background: #fff;
    border: 3px solid rgba(185,156,255,0.35);
    border-radius: 24px;
    padding: 12px 8px;
    min-height: 78px;
    font-size: clamp(18px, 3vw, 22px);
    font-weight: 950;
    color: #6b5080;
    box-shadow: 0 6px 16px rgba(119, 76, 128, 0.10);
  }

  .theme-card.selected {
    border-color: #ff8cc6;
    background: #fff4fb;
    box-shadow: 0 8px 18px rgba(255, 140, 198, 0.25);
  }

  .mode-grid {
    display: grid;
    grid-template-columns: repeat(3, 1fr);
    gap: 12px;
  }

  .game-grid {
    display: grid;
    grid-template-columns: 0.92fr 1.08fr;
    gap: 18px;
    align-items: start;
  }

  .progress {
    display: flex;
    gap: 8px;
    margin: 10px 0 14px;
    justify-content: center;
  }

  .dot {
    width: 34px;
    height: 34px;
    border-radius: 50%;
    background: #fff;
    border: 4px solid rgba(185,156,255,0.36);
    box-shadow: inset 0 0 0 3px rgba(255,255,255,0.7);
  }

  .dot.done:nth-child(1) { background: var(--pink); }
  .dot.done:nth-child(2) { background: var(--yellow); }
  .dot.done:nth-child(3) { background: var(--blue); }
  .dot.done:nth-child(4) { background: var(--green); }
  .dot.done:nth-child(5) { background: var(--purple); }

  .question-card {
    min-height: 520px;
    display: flex;
    flex-direction: column;
  }

  .q-label {
    display: inline-flex;
    align-self: flex-start;
    border-radius: 999px;
    padding: 8px 13px;
    font-size: 16px;
    font-weight: 950;
    color: #735079;
    background: #fff2fb;
    border: 2px solid rgba(255, 140, 198, 0.26);
    margin-bottom: 12px;
  }

  .question {
    font-size: clamp(27px, 5vw, 44px);
    line-height: 1.25;
    font-weight: 1000;
    margin: 4px 0 16px;
  }

  .choices {
    display: grid;
    grid-template-columns: 1fr;
    gap: 12px;
    margin-top: auto;
  }

  .choice {
    min-height: 72px;
    border-radius: 22px;
    background: #fff;
    color: #4a3b4f;
    font-size: clamp(24px, 4.4vw, 36px);
    font-weight: 1000;
    border: 4px solid rgba(131, 201, 255, 0.35);
    box-shadow: 0 7px 16px rgba(119, 76, 128, 0.10);
    padding: 12px;
  }

  .choice:active {
    transform: scale(0.99);
  }

  .choice.correct {
    background: #effff3;
    border-color: #76dc99;
  }

  .choice.wrong {
    background: #fff1f1;
    border-color: #ff8f8f;
  }

  .feedback {
    min-height: 78px;
    margin: 12px 0 0;
    border-radius: 22px;
    padding: 13px 15px;
    background: rgba(255,255,255,0.7);
    border: 3px dashed rgba(185,156,255,0.24);
    color: #6b5080;
    font-size: clamp(18px, 3.2vw, 24px);
    line-height: 1.35;
    font-weight: 900;
  }

  .next-row {
    display: none;
    margin-top: 12px;
  }

  .next-row.show {
    display: grid;
  }

  .mini-status {
    display: flex;
    justify-content: space-between;
    gap: 8px;
    flex-wrap: wrap;
    margin-top: 11px;
    font-size: 16px;
    color: var(--muted);
    font-weight: 800;
  }

  .finish {
    text-align: center;
  }

  .finish h2 {
    font-size: clamp(34px, 6vw, 58px);
    margin: 8px 0 8px;
  }

  .finish p {
    font-size: clamp(19px, 3vw, 26px);
    color: var(--muted);
    font-weight: 800;
    line-height: 1.5;
  }

  .parent-note {
    margin-top: 16px;
    border-top: 2px dashed rgba(119,76,128,0.18);
    padding-top: 14px;
    color: #6d6272;
    font-size: 15px;
    line-height: 1.55;
    user-select: text;
  }

  .stats-grid {
    display: grid;
    grid-template-columns: repeat(3, 1fr);
    gap: 12px;
    margin: 14px 0;
  }

  .stat {
    background: #fff;
    border-radius: 22px;
    padding: 14px;
    border: 2px solid rgba(185,156,255,0.24);
  }

  .stat .num {
    font-size: 31px;
    font-weight: 1000;
    color: #7b4b80;
  }

  .stat .cap {
    color: var(--muted);
    font-weight: 850;
  }

  .skill-list {
    display: grid;
    gap: 10px;
    margin: 12px 0;
  }

  .skill-row {
    background: #fff;
    border-radius: 18px;
    padding: 12px;
    border: 2px solid rgba(131,201,255,0.22);
  }

  .skill-top {
    display: flex;
    justify-content: space-between;
    font-weight: 900;
    gap: 8px;
  }

  .bar {
    height: 14px;
    background: #f2edf8;
    border-radius: 999px;
    margin-top: 8px;
    overflow: hidden;
  }

  .bar-fill {
    height: 100%;
    width: 0%;
    background: linear-gradient(90deg, #ff8cc6, #83c9ff);
    border-radius: 999px;
  }

  .mistake-list {
    background: #fff;
    border-radius: 22px;
    padding: 14px;
    border: 2px solid rgba(255,140,198,0.22);
    font-size: 16px;
    line-height: 1.55;
    user-select: text;
  }

  .mistake-list li {
    margin-bottom: 8px;
  }

  .footer-actions {
    display: grid;
    grid-template-columns: 1fr 1fr;
    gap: 12px;
    margin-top: 16px;
  }

  .sparkle {
    position: fixed;
    pointer-events: none;
    font-size: 30px;
    animation: pop 800ms ease-out forwards;
    z-index: 99;
  }

  @keyframes pop {
    0% { transform: translateY(0) scale(0.4) rotate(0deg); opacity: 0; }
    20% { opacity: 1; }
    100% { transform: translateY(-80px) scale(1.25) rotate(18deg); opacity: 0; }
  }

  @media (max-width: 760px) {
    body { padding: 10px; }
    .hero, .game-grid {
      grid-template-columns: 1fr;
    }
    .theme-grid {
      grid-template-columns: repeat(2, 1fr);
    }
    .mode-grid {
      grid-template-columns: 1fr;
    }
    .stats-grid {
      grid-template-columns: 1fr;
    }
    .question-card {
      min-height: auto;
    }
    .paint-frame {
      max-width: 470px;
      margin: 0 auto;
    }
  }
</style>
</head>
<body>
<div class="app">
  <div class="topbar">
    <div class="brand">🎨 みわの ぬりえクイズ</div>
    <button id="homeBtn" class="btn secondary small">おうち</button>
  </div>

  <section id="homeScreen" class="screen active">
    <div class="card hero">
      <div>
        <h1 class="title">といて、ぬって、かんせい！</h1>
        <p class="subtitle">さんすう・こくごを とくと、えが すこしずつ かわいくなるよ。</p>

        <div class="big-buttons">
          <button id="startBtn" class="btn">はじめる ✨</button>
          <button id="weakBtn" class="btn green">にがて れんしゅう 🌱</button>
          <button id="statsBtn" class="btn secondary">とーと用 きろく 📊</button>
        </div>
      </div>
      <div class="paint-frame" id="homeArt"></div>
    </div>
  </section>

  <section id="setupScreen" class="screen">
    <div class="card">
      <h2 class="title" style="font-size:clamp(30px,6vw,52px);">どの えにする？</h2>
      <div id="themeGrid" class="theme-grid"></div>

      <h2 class="title" style="font-size:clamp(26px,5vw,42px); margin-top:16px;">なにを やる？</h2>
      <div class="mode-grid">
        <button data-mode="math" class="btn yellow modeBtn">さんすう</button>
        <button data-mode="jp" class="btn green modeBtn">こくご</button>
        <button data-mode="mix" class="btn modeBtn">まぜまぜ</button>
      </div>
      <div class="parent-note">
        とーと用メモ：通常モードでも、過去に間違えた問題が少しだけ混ざります。遊びながら復習する仕組みです。
      </div>
    </div>
  </section>

  <section id="gameScreen" class="screen">
    <div class="game-grid">
      <div class="card">
        <div class="paint-frame" id="gameArt"></div>
        <div class="progress" id="progressDots"></div>
        <div class="mini-status">
          <span id="attemptStatus">0もん</span>
          <span id="themeStatus">テーマ</span>
        </div>
      </div>

      <div class="card question-card">
        <div id="qLabel" class="q-label">さんすう</div>
        <div id="questionText" class="question">もんだい</div>
        <div id="choices" class="choices"></div>
        <div id="feedback" class="feedback">こたえを えらんでね。</div>
        <div id="nextRow" class="next-row">
          <button id="nextBtn" class="btn">つぎへ</button>
        </div>
      </div>
    </div>
  </section>

  <section id="finishScreen" class="screen">
    <div class="card finish">
      <div class="paint-frame" id="finishArt" style="max-width:520px; margin:0 auto 14px;"></div>
      <h2 id="finishTitle">かんせい！</h2>
      <p id="finishText">よくがんばったね。</p>
      <div class="footer-actions">
        <button id="againBtn" class="btn">もう1かい</button>
        <button id="finishStatsBtn" class="btn secondary">きろくをみる</button>
      </div>
    </div>
  </section>

  <section id="statsScreen" class="screen">
    <div class="card">
      <h2 class="title" style="font-size:clamp(30px,6vw,52px);">とーと用 きろく</h2>
      <p class="subtitle" style="margin-bottom:8px;">正解率・苦手・集中時間をこのiPad内に保存します。</p>

      <div class="stats-grid">
        <div class="stat"><div id="statSessions" class="num">0</div><div class="cap">プレイ回数</div></div>
        <div class="stat"><div id="statAccuracy" class="num">0%</div><div class="cap">全体正解率</div></div>
        <div class="stat"><div id="statTime" class="num">0分</div><div class="cap">合計時間</div></div>
      </div>

      <h3>分野別</h3>
      <div id="skillList" class="skill-list"></div>

      <h3>最近のにがて</h3>
      <ol id="mistakeList" class="mistake-list"></ol>

      <div class="footer-actions">
        <button id="backHomeBtn" class="btn">もどる</button>
        <button id="resetBtn" class="btn secondary">きろくをリセット</button>
      </div>

      <div class="parent-note">
        使い方：正解率が低い分野を長く練習させるより、翌日に「にがて れんしゅう」を1回だけ入れるのがおすすめです。短く、楽しく、少し戻すのが続きます。
      </div>
    </div>
  </section>
</div>

<script>
(function() {
  var STORE_KEY = "miwa_nurie_quiz_v2";
  var palette = ["#ff8cc6", "#ffe08a", "#83c9ff", "#9be7b0", "#b99cff"];
  var selectedTheme = "cake";
  var currentScreen = "homeScreen";
  var unlockedSpeech = false;

  var skillNames = {
    math_add: "たしざん",
    math_sub: "ひきざん",
    number_sense: "かずの感覚",
    kana: "ひらがな",
    vocab: "ことば",
    reading: "文のりかい"
  };

  var data = loadData();
  var session = null;

  var themes = {
    cake: {
      name: "ケーキ",
      emoji: "🎂",
      svg: cakeSvg
    },
    cat: {
      name: "ねこ",
      emoji: "🐱",
      svg: catSvg
    },
    flower: {
      name: "おはな",
      emoji: "🌷",
      svg: flowerSvg
    },
    bunny: {
      name: "うさぎ",
      emoji: "🐰",
      svg: bunnySvg
    }
  };

  function $(id) {
    return document.getElementById(id);
  }

  function loadData() {
    try {
      var raw = localStorage.getItem(STORE_KEY);
      if (raw) return JSON.parse(raw);
    } catch (e) {}
    return {
      sessions: 0,
      total: 0,
      correct: 0,
      totalSeconds: 0,
      skills: {},
      mistakes: [],
      themes: {},
      lastPlayed: ""
    };
  }

  function saveData() {
    try {
      localStorage.setItem(STORE_KEY, JSON.stringify(data));
    } catch (e) {}
  }

  function bindTap(el, fn) {
    var lastTouch = 0;
    el.addEventListener("touchend", function(e) {
      e.preventDefault();
      lastTouch = Date.now();
      unlockSpeech();
      fn(e);
    }, { passive: false });
    el.addEventListener("click", function(e) {
      if (Date.now() - lastTouch < 600) return;
      unlockSpeech();
      fn(e);
    });
  }

  function unlockSpeech() {
    if (unlockedSpeech) return;
    unlockedSpeech = true;
    try {
      if (window.speechSynthesis) {
        var u = new SpeechSynthesisUtterance("");
        u.lang = "ja-JP";
        u.volume = 0;
        window.speechSynthesis.speak(u);
      }
    } catch (e) {}
  }

  function chooseVoice() {
    if (!window.speechSynthesis) return null;
    var voices = window.speechSynthesis.getVoices ? window.speechSynthesis.getVoices() : [];
    var i, v, name;
    for (i = 0; i < voices.length; i++) {
      v = voices[i];
      name = (v.name || "").toLowerCase();
      if ((name.indexOf("siri") >= 0 || name.indexOf("kyoko") >= 0) && (v.lang || "").indexOf("ja") === 0) return v;
    }
    for (i = 0; i < voices.length; i++) {
      v = voices[i];
      if ((v.lang || "").indexOf("ja") === 0 && v.localService) return v;
    }
    for (i = 0; i < voices.length; i++) {
      v = voices[i];
      if ((v.lang || "").indexOf("ja") === 0) return v;
    }
    return null;
  }

  function speak(text) {
    try {
      if (!window.speechSynthesis || !unlockedSpeech) return;
      window.speechSynthesis.cancel();
      var u = new SpeechSynthesisUtterance(text);
      u.lang = "ja-JP";
      u.pitch = 1.1;
      u.rate = 1.0;
      var voice = chooseVoice();
      if (voice) u.voice = voice;
      window.speechSynthesis.speak(u);
    } catch (e) {}
  }

  function showScreen(id) {
    var screens = document.querySelectorAll(".screen");
    for (var i = 0; i < screens.length; i++) screens[i].classList.remove("active");
    $(id).classList.add("active");
    currentScreen = id;
  }

  function renderTheme(containerId, themeKey, filledCount) {
    var html = themes[themeKey].svg();
    $(containerId).innerHTML = html;
    var parts = $("#" + containerId) ? $(containerId).querySelectorAll("[data-part]") : [];
    for (var i = 0; i < parts.length; i++) {
      var partNum = parseInt(parts[i].getAttribute("data-part"), 10);
      if (partNum < filledCount) {
        parts[i].setAttribute("fill", palette[partNum % palette.length]);
      } else {
        parts[i].setAttribute("fill", "#ffffff");
      }
    }
  }

  function fillPart(partIndex) {
    var nodes = $("gameArt").querySelectorAll('[data-part="' + partIndex + '"]');
    for (var i = 0; i < nodes.length; i++) {
      nodes[i].setAttribute("fill", palette[partIndex % palette.length]);
      nodes[i].style.transform = "scale(1.025)";
      (function(n) {
        setTimeout(function(){ n.style.transform = "scale(1)"; }, 260);
      })(nodes[i]);
    }
  }

  function renderHomeArt() {
    renderTheme("homeArt", "cake", 5);
  }

  function renderThemeGrid() {
    var grid = $("themeGrid");
    grid.innerHTML = "";
    for (var key in themes) {
      if (!themes.hasOwnProperty(key)) continue;
      var b = document.createElement("button");
      b.className = "theme-card" + (key === selectedTheme ? " selected" : "");
      b.setAttribute("data-theme", key);
      b.innerHTML = '<div style="font-size:30px;">' + themes[key].emoji + '</div><div>' + themes[key].name + '</div>';
      bindTap(b, function(e) {
        var t = e.currentTarget.getAttribute("data-theme");
        selectedTheme = t;
        renderThemeGrid();
        speak(themes[t].name + "にするね");
      });
      grid.appendChild(b);
    }
  }

  function startSession(mode) {
    var useMode = mode;
    if (mode === "weak" && data.mistakes.length === 0) useMode = "mix";

    session = {
      mode: useMode,
      requestedMode: mode,
      theme: selectedTheme,
      correctParts: 0,
      attempts: 0,
      maxAttempts: 8,
      targetParts: 5,
      start: Date.now(),
      current: null,
      answered: false,
      correctInSession: 0
    };

    renderTheme("gameArt", session.theme, 0);
    $("themeStatus").textContent = themes[session.theme].emoji + " " + themes[session.theme].name;
    renderDots();
    showScreen("gameScreen");
    nextQuestion();
    speak("はじめるよ");
  }

  function nextQuestion() {
    session.answered = false;
    $("nextRow").classList.remove("show");
    $("feedback").textContent = "こたえを えらんでね。";
    $("choices").innerHTML = "";
    session.current = makeQuestionForMode(session.mode);
    session.attempts++;
    $("attemptStatus").textContent = session.attempts + "もんめ";
    renderQuestion(session.current);
  }

  function renderQuestion(q) {
    $("qLabel").textContent = q.label;
    $("questionText").textContent = q.prompt;

    var choices = $("choices");
    choices.innerHTML = "";
    for (var i = 0; i < q.choices.length; i++) {
      var b = document.createElement("button");
      b.className = "choice";
      b.textContent = q.choices[i];
      b.setAttribute("data-answer", q.choices[i]);
      bindTap(b, onChoice);
      choices.appendChild(b);
    }
  }

  function onChoice(e) {
    if (!session || session.answered) return;
    session.answered = true;

    var selected = e.currentTarget.getAttribute("data-answer");
    var q = session.current;
    var ok = selected === q.answer;
    var btns = $("choices").querySelectorAll(".choice");

    for (var i = 0; i < btns.length; i++) {
      var a = btns[i].getAttribute("data-answer");
      if (a === q.answer) btns[i].classList.add("correct");
      if (a === selected && !ok) btns[i].classList.add("wrong");
    }

    recordAnswer(q, ok);

    if (ok) {
      var part = session.correctParts;
      session.correctParts++;
      session.correctInSession++;
      fillPart(part);
      renderDots();
      $("feedback").textContent = praiseText() + "　" + q.explain;
      speak("せいかい。すごいね");
      sparkle();
    } else {
      $("feedback").textContent = "おしい！ こたえは「" + q.answer + "」。" + q.explain;
      speak("おしい。だいじょうぶ");
    }

    if (session.correctParts >= session.targetParts) {
      setTimeout(finishSession, 900);
    } else if (session.attempts >= session.maxAttempts) {
      // 最後は前向きに終わらせる。完成まであと少しの場合は、かんたん問題を出す。
      if (session.correctParts >= 3) {
        session.maxAttempts += 2;
        $("nextBtn").textContent = "あとすこし";
        $("nextRow").classList.add("show");
      } else {
        setTimeout(finishSession, 900);
      }
    } else {
      $("nextBtn").textContent = "つぎへ";
      $("nextRow").classList.add("show");
    }
  }

  function finishSession() {
    var seconds = Math.max(1, Math.round((Date.now() - session.start) / 1000));
    data.sessions++;
    data.totalSeconds += seconds;
    data.lastPlayed = new Date().toISOString();

    if (!data.themes[session.theme]) data.themes[session.theme] = 0;
    data.themes[session.theme]++;

    saveData();

    renderTheme("finishArt", session.theme, session.correctParts);
    if (session.correctParts >= 5) {
      $("finishTitle").textContent = "かんせい！ はなまる！";
      $("finishText").textContent = session.attempts + "もん がんばったよ。ぬりえが できたね。";
      speak("かんせい。はなまる");
    } else {
      $("finishTitle").textContent = "ここまで よくできたね！";
      $("finishText").textContent = session.attempts + "もん がんばったよ。つぎは もうすこし ぬってみよう。";
      speak("よくがんばったね");
    }
    showScreen("finishScreen");
  }

  function renderDots() {
    var p = $("progressDots");
    p.innerHTML = "";
    for (var i = 0; i < 5; i++) {
      var d = document.createElement("div");
      d.className = "dot" + (i < session.correctParts ? " done" : "");
      p.appendChild(d);
    }
  }

  function recordAnswer(q, ok) {
    data.total++;
    if (ok) data.correct++;

    if (!data.skills[q.skill]) data.skills[q.skill] = { total: 0, correct: 0 };
    data.skills[q.skill].total++;
    if (ok) data.skills[q.skill].correct++;

    if (!ok) {
      var m = {
        prompt: q.prompt,
        choices: q.choices.slice(0),
        answer: q.answer,
        skill: q.skill,
        label: q.label,
        explain: q.explain,
        time: new Date().toISOString()
      };
      data.mistakes.unshift(m);
      data.mistakes = uniqueMistakes(data.mistakes).slice(0, 20);
    } else {
      // 同じ問題を正解したら、にがてリストから少し消す
      for (var i = data.mistakes.length - 1; i >= 0; i--) {
        if (data.mistakes[i].prompt === q.prompt && data.mistakes[i].answer === q.answer) {
          data.mistakes.splice(i, 1);
          break;
        }
      }
    }
    saveData();
  }

  function uniqueMistakes(list) {
    var seen = {};
    var out = [];
    for (var i = 0; i < list.length; i++) {
      var key = list[i].prompt + "|" + list[i].answer;
      if (!seen[key]) {
        seen[key] = true;
        out.push(list[i]);
      }
    }
    return out;
  }

  function makeQuestionForMode(mode) {
    // 通常モードでも25%だけ「にがて」を戻す。これがフィードバックループ。
    if (mode !== "weak" && data.mistakes.length > 0 && Math.random() < 0.25) {
      return questionFromMistake();
    }
    if (mode === "weak") {
      if (data.mistakes.length > 0 && Math.random() < 0.75) return questionFromMistake();
      return makeEasyQuestion();
    }
    if (mode === "math") return makeMathQuestion();
    if (mode === "jp") return makeJapaneseQuestion();
    return Math.random() < 0.5 ? makeMathQuestion() : makeJapaneseQuestion();
  }

  function questionFromMistake() {
    var m = data.mistakes[Math.floor(Math.random() * data.mistakes.length)];
    var choices = shuffle(m.choices.slice(0));
    return {
      prompt: m.prompt,
      choices: choices,
      answer: m.answer,
      skill: m.skill,
      label: "にがて ふくしゅう",
      explain: m.explain || "もういちど やってみよう。"
    };
  }

  function makeEasyQuestion() {
    if (Math.random() < 0.5) {
      var a = rand(1, 4);
      var b = rand(1, 3);
      var ans = a + b;
      return buildQuestion("さんすう", "math_add", a + " + " + b + " = ？", ans, "あわせると " + ans + " だね。");
    }
    return makeJapaneseQuestion();
  }

  function makeMathQuestion() {
    var r = Math.random();
    if (r < 0.35) return makeAddQuestion();
    if (r < 0.65) return makeSubQuestion();
    if (r < 0.82) return makeNumberSenseQuestion();
    return makeWordMathQuestion();
  }

  function makeAddQuestion() {
    var max = skillStrong("math_add") ? 8 : 5;
    var a = rand(1, max);
    var b = rand(1, max);
    if (a + b > 12) b = 12 - a;
    if (b < 1) b = 1;
    var ans = a + b;
    return buildQuestion("さんすう", "math_add", a + " + " + b + " = ？", ans, a + " と " + b + " を あわせると " + ans + "。");
  }

  function makeSubQuestion() {
    var max = skillStrong("math_sub") ? 12 : 8;
    var a = rand(3, max);
    var b = rand(1, a - 1);
    var ans = a - b;
    return buildQuestion("さんすう", "math_sub", a + " - " + b + " = ？", ans, a + " から " + b + " を とると " + ans + "。");
  }

  function makeNumberSenseQuestion() {
    var a = rand(1, 12);
    var b = rand(1, 12);
    while (b === a) b = rand(1, 12);
    var bigger = Math.max(a, b);
    var smaller = Math.min(a, b);
    if (Math.random() < 0.5) {
      return {
        label: "かず",
        skill: "number_sense",
        prompt: "おおきい かずは どっち？",
        choices: shuffle([String(a), String(b)]),
        answer: String(bigger),
        explain: bigger + " のほうが おおきいね。"
      };
    }
    return {
      label: "かず",
      skill: "number_sense",
      prompt: "ちいさい かずは どっち？",
      choices: shuffle([String(a), String(b)]),
      answer: String(smaller),
      explain: smaller + " のほうが ちいさいね。"
    };
  }

  function makeWordMathQuestion() {
    var a = rand(2, 7);
    var b = rand(1, 4);
    if (Math.random() < 0.5) {
      var ans = a + b;
      return buildQuestion("ぶんしょうだい", "math_add", "りんごが " + a + "こ。さらに " + b + "こ。ぜんぶで？", ans, "ぜんぶで " + ans + "こ だね。");
    } else {
      if (b >= a) b = a - 1;
      var ans2 = a - b;
      return buildQuestion("ぶんしょうだい", "math_sub", "あめが " + a + "こ。" + b + "こ たべた。のこりは？", ans2, "のこりは " + ans2 + "こ だね。");
    }
  }

  function makeJapaneseQuestion() {
    var r = Math.random();
    if (r < 0.34) return makeKanaQuestion();
    if (r < 0.67) return makeVocabQuestion();
    return makeReadingQuestion();
  }

  function makeKanaQuestion() {
    var words = [
      ["ねこ", "ね"], ["いぬ", "い"], ["さくら", "さ"], ["りんご", "り"],
      ["くるま", "く"], ["ほし", "ほ"], ["はな", "は"], ["うさぎ", "う"],
      ["みかん", "み"], ["けーき", "け"]
    ];
    var item = words[rand(0, words.length - 1)];
    var all = ["あ","い","う","え","お","か","き","く","け","こ","さ","し","す","せ","そ","ね","り","は","ほ","み"];
    var choices = [item[1]];
    while (choices.length < 4) {
      var c = all[rand(0, all.length - 1)];
      if (choices.indexOf(c) < 0) choices.push(c);
    }
    return {
      label: "ひらがな",
      skill: "kana",
      prompt: "「" + item[0] + "」の はじめの もじは？",
      choices: shuffle(choices),
      answer: item[1],
      explain: "「" + item[0] + "」は「" + item[1] + "」から はじまるね。"
    };
  }

  function makeVocabQuestion() {
    var sets = [
      { prompt: "「ねこ」と おなじ なかまは？", answer: "いぬ", choices: ["いぬ", "くるま", "けーき", "ぼうし"], explain: "ねこも いぬも どうぶつだね。" },
      { prompt: "「りんご」と おなじ なかまは？", answer: "みかん", choices: ["みかん", "つくえ", "くつ", "えんぴつ"], explain: "りんごも みかんも くだものだね。" },
      { prompt: "「あか」と おなじ なかまは？", answer: "あお", choices: ["あお", "ねこ", "ごはん", "はしる"], explain: "あかも あおも いろだね。" },
      { prompt: "「はしる」と おなじ なかまは？", answer: "とぶ", choices: ["とぶ", "りぼん", "さかな", "みどり"], explain: "はしるも とぶも うごきだね。" },
      { prompt: "「おはな」は どこに さく？", answer: "にわ", choices: ["にわ", "そら", "うみ", "れいぞうこ"], explain: "おはなは にわや はたけに さくね。" }
    ];
    var s = sets[rand(0, sets.length - 1)];
    return {
      label: "ことば",
      skill: "vocab",
      prompt: s.prompt,
      choices: shuffle(s.choices.slice(0)),
      answer: s.answer,
      explain: s.explain
    };
  }

  function makeReadingQuestion() {
    var qs = [
      { prompt: "「ねこが ねています。」ねこは なにを してる？", answer: "ねてる", choices: ["ねてる", "とんでる", "ないてる"], explain: "ねこは ねているね。" },
      { prompt: "「あめが ふっています。」ふっているのは？", answer: "あめ", choices: ["あめ", "ゆき", "はな"], explain: "ふっているのは あめだね。" },
      { prompt: "「みわは えを かきます。」みわは なにを かく？", answer: "え", choices: ["え", "ごはん", "くつ"], explain: "みわは えを かくね。" },
      { prompt: "「うさぎが にんじんを たべます。」たべるものは？", answer: "にんじん", choices: ["にんじん", "さかな", "ぼうし"], explain: "うさぎは にんじんを たべるね。" },
      { prompt: "「ほしが きらきら ひかります。」ひかるのは？", answer: "ほし", choices: ["ほし", "ねこ", "りんご"], explain: "ほしが ひかるね。" }
    ];
    var q = qs[rand(0, qs.length - 1)];
    return {
      label: "文のりかい",
      skill: "reading",
      prompt: q.prompt,
      choices: shuffle(q.choices.slice(0)),
      answer: q.answer,
      explain: q.explain
    };
  }

  function buildQuestion(label, skill, prompt, numericAnswer, explain) {
    var ans = String(numericAnswer);
    var choices = [ans];
    var delta = [-2, -1, 1, 2, 3, -3];
    for (var i = 0; i < delta.length && choices.length < 4; i++) {
      var v = numericAnswer + delta[i];
      if (v >= 0 && choices.indexOf(String(v)) < 0) choices.push(String(v));
    }
    while (choices.length < 4) {
      var r = rand(0, 12);
      if (choices.indexOf(String(r)) < 0) choices.push(String(r));
    }
    return {
      label: label,
      skill: skill,
      prompt: prompt,
      choices: shuffle(choices),
      answer: ans,
      explain: explain
    };
  }

  function skillStrong(skill) {
    var s = data.skills[skill];
    if (!s || s.total < 8) return false;
    return (s.correct / s.total) >= 0.8;
  }

  function renderStats() {
    $("statSessions").textContent = data.sessions;
    var acc = data.total ? Math.round((data.correct / data.total) * 100) : 0;
    $("statAccuracy").textContent = acc + "%";
    $("statTime").textContent = Math.round(data.totalSeconds / 60) + "分";

    var list = $("skillList");
    list.innerHTML = "";
    var any = false;
    for (var key in skillNames) {
      if (!skillNames.hasOwnProperty(key)) continue;
      var s = data.skills[key] || { total: 0, correct: 0 };
      var rate = s.total ? Math.round((s.correct / s.total) * 100) : 0;
      var row = document.createElement("div");
      row.className = "skill-row";
      row.innerHTML = '<div class="skill-top"><span>' + skillNames[key] + '</span><span>' + s.correct + '/' + s.total + '　' + rate + '%</span></div><div class="bar"><div class="bar-fill" style="width:' + rate + '%"></div></div>';
      list.appendChild(row);
      any = true;
    }
    if (!any) list.textContent = "まだ記録がありません。";

    var ml = $("mistakeList");
    ml.innerHTML = "";
    if (data.mistakes.length === 0) {
      var li = document.createElement("li");
      li.textContent = "今のところ、にがて記録はありません。";
      ml.appendChild(li);
    } else {
      var max = Math.min(8, data.mistakes.length);
      for (var i = 0; i < max; i++) {
        var m = data.mistakes[i];
        var li2 = document.createElement("li");
        li2.textContent = "【" + (skillNames[m.skill] || m.label) + "】" + m.prompt + " → 答え：" + m.answer;
        ml.appendChild(li2);
      }
    }
  }

  function praiseText() {
    var arr = [
      "すごい！",
      "いいね！",
      "よくみてる！",
      "さすが！",
      "はなまる！",
      "そのちょうし！",
      "きれいに ぬれたね！"
    ];
    return arr[rand(0, arr.length - 1)];
  }

  function sparkle() {
    var marks = ["✨","🌟","💖","🎨","💮"];
    for (var i = 0; i < 8; i++) {
      var sp = document.createElement("div");
      sp.className = "sparkle";
      sp.textContent = marks[rand(0, marks.length - 1)];
      sp.style.left = rand(12, 88) + "vw";
      sp.style.top = rand(24, 80) + "vh";
      sp.style.animationDelay = (i * 45) + "ms";
      document.body.appendChild(sp);
      (function(node) {
        setTimeout(function(){ if (node.parentNode) node.parentNode.removeChild(node); }, 1100);
      })(sp);
    }
  }

  function shuffle(arr) {
    for (var i = arr.length - 1; i > 0; i--) {
      var j = Math.floor(Math.random() * (i + 1));
      var t = arr[i];
      arr[i] = arr[j];
      arr[j] = t;
    }
    return arr;
  }

  function rand(min, max) {
    return Math.floor(Math.random() * (max - min + 1)) + min;
  }

  function cakeSvg() {
    return '<svg viewBox="0 0 420 420" aria-label="cake">' +
      '<ellipse data-part="0" class="outline" cx="210" cy="340" rx="145" ry="38"/>' +
      '<rect data-part="1" class="outline" x="95" y="185" width="230" height="135" rx="26"/>' +
      '<path data-part="2" class="outline" d="M95 205 C120 160, 150 220, 180 185 C210 150, 235 220, 265 185 C295 150, 320 190, 325 205 L325 230 L95 230 Z"/>' +
      '<rect data-part="3" class="outline" x="195" y="100" width="30" height="82" rx="10"/>' +
      '<path data-part="3" class="outline thin" d="M210 80 C190 105, 215 116, 210 135 C232 112, 235 94, 210 80 Z"/>' +
      '<path data-part="4" class="outline" d="M210 275 C190 250, 155 265, 163 296 C170 325, 210 338, 210 338 C210 338, 250 325, 257 296 C265 265, 230 250, 210 275 Z"/>' +
      '<circle class="face" cx="170" cy="260" r="1"/><circle class="face" cx="250" cy="260" r="1"/><path class="face" d="M190 286 Q210 302 230 286"/>' +
      '</svg>';
  }

  function catSvg() {
    return '<svg viewBox="0 0 420 420" aria-label="cat">' +
      '<path data-part="0" class="outline" d="M115 220 C115 155, 160 115, 210 115 C260 115, 305 155, 305 220 L305 310 C305 350, 270 365, 210 365 C150 365, 115 350, 115 310 Z"/>' +
      '<path data-part="1" class="outline" d="M128 150 L100 80 L170 120 Z"/>' +
      '<path data-part="1" class="outline" d="M292 150 L320 80 L250 120 Z"/>' +
      '<circle data-part="2" class="outline" cx="210" cy="220" r="82"/>' +
      '<path data-part="3" class="outline" d="M115 285 C60 275, 60 210, 112 215 C140 218, 143 250, 120 262"/>' +
      '<path data-part="4" class="outline" d="M180 286 C195 305, 225 305, 240 286"/>' +
      '<circle class="face" cx="180" cy="210" r="1"/><circle class="face" cx="240" cy="210" r="1"/><path class="face" d="M205 235 L215 235 L210 245 Z"/><path class="face" d="M165 240 L120 230 M165 255 L120 260 M255 240 L300 230 M255 255 L300 260"/>' +
      '</svg>';
  }

  function flowerSvg() {
    return '<svg viewBox="0 0 420 420" aria-label="flower">' +
      '<path data-part="0" class="outline" d="M210 125 C190 65, 230 65, 210 125 Z"/>' +
      '<path data-part="0" class="outline" d="M210 125 C250 80, 280 120, 225 145 Z"/>' +
      '<path data-part="0" class="outline" d="M210 125 C275 145, 250 190, 220 155 Z"/>' +
      '<path data-part="0" class="outline" d="M210 125 C170 80, 140 120, 195 145 Z"/>' +
      '<path data-part="0" class="outline" d="M210 125 C145 145, 170 190, 200 155 Z"/>' +
      '<circle data-part="1" class="outline" cx="210" cy="145" r="34"/>' +
      '<path data-part="2" class="outline" d="M210 178 C210 220, 210 275, 210 330"/>' +
      '<path data-part="3" class="outline" d="M210 250 C160 225, 135 245, 120 285 C165 290, 195 278, 210 250 Z"/>' +
      '<path data-part="3" class="outline" d="M210 270 C260 245, 285 260, 300 300 C252 305, 225 295, 210 270 Z"/>' +
      '<path data-part="4" class="outline" d="M120 330 L300 330 L275 390 L145 390 Z"/>' +
      '<circle class="face" cx="198" cy="140" r="1"/><circle class="face" cx="222" cy="140" r="1"/><path class="face" d="M198 155 Q210 166 222 155"/>' +
      '</svg>';
  }

  function bunnySvg() {
    return '<svg viewBox="0 0 420 420" aria-label="bunny">' +
      '<ellipse data-part="0" class="outline" cx="165" cy="115" rx="34" ry="88" transform="rotate(-16 165 115)"/>' +
      '<ellipse data-part="0" class="outline" cx="255" cy="115" rx="34" ry="88" transform="rotate(16 255 115)"/>' +
      '<circle data-part="1" class="outline" cx="210" cy="235" r="105"/>' +
      '<ellipse data-part="2" class="outline" cx="150" cy="258" rx="38" ry="30"/>' +
      '<ellipse data-part="2" class="outline" cx="270" cy="258" rx="38" ry="30"/>' +
      '<path data-part="3" class="outline" d="M170 340 C155 382, 205 392, 210 352 C215 392, 265 382, 250 340 Z"/>' +
      '<path data-part="4" class="outline" d="M210 275 C190 255, 170 275, 185 298 C198 318, 210 320, 210 320 C210 320, 222 318, 235 298 C250 275, 230 255, 210 275 Z"/>' +
      '<circle class="face" cx="175" cy="218" r="1"/><circle class="face" cx="245" cy="218" r="1"/><path class="face" d="M205 240 L215 240 L210 250 Z"/><path class="face" d="M195 268 Q210 282 225 268"/>' +
      '</svg>';
  }

  function init() {
    renderHomeArt();
    renderThemeGrid();

    bindTap($("homeBtn"), function() {
      showScreen("homeScreen");
    });
    bindTap($("startBtn"), function() {
      renderThemeGrid();
      showScreen("setupScreen");
    });
    bindTap($("weakBtn"), function() {
      startSession("weak");
    });
    bindTap($("statsBtn"), function() {
      renderStats();
      showScreen("statsScreen");
    });
    bindTap($("nextBtn"), function() {
      nextQuestion();
    });
    bindTap($("againBtn"), function() {
      renderThemeGrid();
      showScreen("setupScreen");
    });
    bindTap($("finishStatsBtn"), function() {
      renderStats();
      showScreen("statsScreen");
    });
    bindTap($("backHomeBtn"), function() {
      showScreen("homeScreen");
    });
    bindTap($("resetBtn"), function() {
      if (confirm("記録をリセットしますか？")) {
        try { localStorage.removeItem(STORE_KEY); } catch(e) {}
        data = loadData();
        renderStats();
      }
    });

    var modeBtns = document.querySelectorAll(".modeBtn");
    for (var i = 0; i < modeBtns.length; i++) {
      bindTap(modeBtns[i], function(e) {
        startSession(e.currentTarget.getAttribute("data-mode"));
      });
    }

    if (window.speechSynthesis && window.speechSynthesis.onvoiceschanged !== undefined) {
      window.speechSynthesis.onvoiceschanged = function(){ chooseVoice(); };
    }
  }

  init();
})();
</script>
</body>
</html>
